Building Pipeline:
1> Create a GitHub repo and clone it in local (Add experiments).

2> Open GitBash and Go to C:\Users\sudip\MLOpsGuestLecture
3> code .  --> This will open Visual Studio

4> Create 'src' folder and create following files 
data_ingestion.py      --> This is for ingesting the data from 'spam.csv'
data_preprocessing.py  --> This is for pre-processing the data. This will create a 'data\interim' folder, that will have the cleaned & formatted text data
feature_engineering.py --> Apply TF-IDF (statistical weighting method used in NLP and search algorithms to determine a word's relevance to a specific document.
                           This will create a 'data\processed' folder, that will have the formatted vector dataset 
model_building.py      --> Training of the model (random forest). This will create a 'models' folder
model_evaluation.py    --> Calculates the evaluation metrics based on the processed-test data and creates a 'reports' folder

5> Install all softwares
----------------------
python -m nltk.downloader punkt_tab
python -m pip install nltk
python -m pip install pyyaml
python -m pip install scikit-learn
python -m pip install pandas
python -m pip install dvclive
python -m pip install nvidia-ml-py

6> Git user name and email must be configured
----------------------------------------------
git config --global user.name "sudipacharya26"
git config --global user.email "sudip_acharya@live.com"


6> Run the following files
---------------------------
python src/data_ingestion.py
python src/data_preprocessing.py
python src/feature_engineering.py
python src/model_building.py
python src/model_evaluation.py


7> Add data, models, reports directories to .gitignore file
8> Commit the files from local env to git repo.
git add .
git commit -m "MLOps Pipeline ready"
git push origin main


Setting up dvc pipeline (without params)
5> Create dvc.yaml file and add stages to it.
6> dvc init then do "dvc repro" to test the pipeline automation. (check dvc dag)
7> Now git add, commit, push

Setting up dvc pipeline (with params)
8> add params.yaml file
9> Add the params setup (mentioned below)
10> Do "dvc repro" again to test the pipeline along with the params
11> Now git add, commit, push

Expermients with DVC:
12> pip install dvclive
13> Add the dvclive code block (mentioned below)
14> Do "dvc exp run", it will create a new dvc.yaml(if already not there) and dvclive directory (each run will be considered as an experiment by DVC)
15> Do "dvc exp show" on terminal to see the experiments or use extension on VSCode (install dvc extension)
16> Do "dvc exp remove {exp-name}" to remove exp (optional) | "dvc exp apply {exp-name}" to reproduce prev exp
17> Change params, re-run code (produce new experiments)
18> Now git add, commit, push


